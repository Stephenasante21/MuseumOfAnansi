using UnityEngine;

public class GameState : MonoBehaviour
{

    public static bool OkomfoPiece = false;
    public static bool HardOkomfoPiece = false;
    public static bool AsantewaaPiece = false;
    public static bool HardAsantewaaPiece = false;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
